#
# This source file is part of the Stanford Spezi open source project
#
# SPDX-FileCopyrightText: 2026 Stanford University and the project authors (see CONTRIBUTORS.md)
#
# SPDX-License-Identifier: MIT
#

apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-name-placeholder-web
  namespace: app-name-placeholder
  labels:
    app: app-name-placeholder-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-name-placeholder-web
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: app-name-placeholder-web
    spec:
      serviceAccountName: app-name-placeholder-web
      containers:
        - name: app-name-placeholder-web-container
          image: ghcr.io/registry-org-placeholder/app-name-placeholder-web:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: 64Mi
              cpu: 25m
            limits:
              memory: 256Mi
              cpu: 100m
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 101
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: app-name-placeholder-web-config
          startupProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
            failureThreshold: 12
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            periodSeconds: 15
