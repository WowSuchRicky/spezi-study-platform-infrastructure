spec:
  template:
    spec:
      containers:
      - name: argocd-server
        env:
        - name: ARGOCD_OIDC_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: argocd-oidc-secret
              key: oidc.keycloak.clientSecret